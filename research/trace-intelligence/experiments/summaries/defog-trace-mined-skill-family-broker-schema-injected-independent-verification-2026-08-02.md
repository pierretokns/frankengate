# Independent verification: family-disjoint broker replay

All 18 external raw audits passed hash, attempt-chain, task/arm identity,
authority/epoch, policy, terminal scheduling, fallback, and unauthorized-
observation checks. A separate semantic verifier independently reran the one
submitted formatting-placebo candidate and its sealed gold alternatives; it
matched the stored semantic result. No candidate was submitted by the no-skill
or trace-mined arms. Raw prompts, SQL, rows, and verifier events remain
external.
