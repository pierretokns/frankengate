# CMU access recheck (2026-07-31)

The local Hugging Face CLI is installed, but its identity check could not
reach the Hub because DNS/network resolution failed. No authenticated identity,
approval, or trajectory download was observed, and no raw CMU data was touched.

The prior admission decision therefore remains unchanged: CMU stays deferred,
not silently admitted. Public Trace Commons, Fable, CodeTraceBench, OTel, RL,
and researcher cohorts remain the executable evidence sources until a separate
authenticated approval and network receipt exists.
